# InterpolationApp iPhone

Minimal SwiftUI iPhone app that mirrors the interpolation table used by the Android variant.

## Build locally

```bash
cd ios/InterpolationApp
xcodebuild -project InterpolationApp.xcodeproj -scheme InterpolationApp -configuration Debug -sdk iphonesimulator build
```
